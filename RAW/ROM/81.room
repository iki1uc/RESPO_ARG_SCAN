function expand81(cube) {
    let block = [];
    cube.forEach(p => {
        block.push(p);
        block.push({ ...p, X: p.X + 1 });
        block.push({ ...p, Y: p.Y + 1 });
    });
    return block;
}
